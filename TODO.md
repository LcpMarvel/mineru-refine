# TODO — 残留 / 搁置 / 明确不做

> 说明：截至 core 0.13.0，前期精修问题（乱码表重转写/降级、单元格 LaTeX、漏标标题子项信号、
> 拉丁 token 频率投票、表格题注误吞标题、caption 家具、VL 贪婪解码确定性、清洗阶段进度钩子等）
> 均已落地，历史完成记录见 git 提交历史，本文件只保留仍待处理 / 搁置观察 / 明确不做的项。

## 待议 / 观察

- **文本侧（DeepSeek）抖动**：DeepSeek 走 tool-call 路径，temperature 已 0，但偶发
  `llm_no_tool_call` / 轮次耗尽会让某疑点这轮落、下轮不落（病灶是模型不调工具，非采样温度，
  `top_k` 治不到）。若要稳，方向是 tool_choice=required 兜底 / 搁置项二次重试——未排期。
- **mergeTable 合表 ≠ 修内容**：跨页合并只接结构，续表单元格内乱码（拆字/并格错位/空格断字）
  原样保留，归 opt-in 的 `rewriteGarbledTables` / `degradeGarbledTables`，默认配置不治。
- **公式碎裂进多格**（JZY 判断矩阵表尾 `一-致性检验：/CI/λmax-/=8.45-8=0.0/645`）：
  跨格重组超出现有 op 能力（cell 级只有整格重转写），搁置。
- **colspan 文本拆裂**（047「批准|表」横拆、「公司核心/指标」竖拆）：
  机械判定「两格拼起来是词而各自不是」可做但收益小，观察。

## 不做（明确排除）

- **漏字补全**（华大科技「00085」缺一位）：要加字且无法从上下文确定加什么，
  只进 report 当质量信号
- **占位符 xxxxxX 归一**：源文档本来的脱敏占位内容，改了反而违背保真
- **页眉/页码清理**：已正确分类为 `header`/`page_number`，下游过滤即可
- **「十一、」「十二、」整句小节升标题**（JZY）：原文这两节就是「编号+整句」体，
  含逗号/句末标点，表面闸正确拒绝——升成 40 字长标题反而更差
- **标题与正文粘连的「1）报告撰写责任：自 2026 年起…」**（047）：需 split+promote
  两步且切分点只能 LLM 判，同级 2）~5）已是标题、此处渲染为段落语义无损，不值得
- **源文档自身的内容错误**（JZY「波特五力」正文把五力写成「买方/卖方议价」重复、
  章节编号 3→5 跳号）：保真原则下不替作者改稿
- **同表双副本去重**（JZY p45「竞争分析表」it_0586 样式展示版 + it_0587 待填写网格版）：
  渲染 origin.pdf 确认是源文档作者主动画了两份同一空模板（品牌标题展示版 + 大号填写网格版），
  非 OCR 重复。删一份 = 替作者改稿，碰保真红线。两份都是空模板、单文档单次、价值有限，且任何
  「相邻近重复表」探测器都有误删合法相似表的数据丢失风险。下游若嫌冗余，自行过滤即可
